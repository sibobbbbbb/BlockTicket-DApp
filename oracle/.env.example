# Server Configuration
PORT=3001
NODE_ENV=development

# Ganache Configuration
RPC_URL=http://127.0.0.1:7545
CHAIN_ID=1337

# Oracle wallet private key (same as Ganache deployer) (WITHOUT 0x)
ORACLE_PRIVATE_KEY=your_ganache_private_key_here_without_0x

# Smart Contract Addresses (from latest deployment) WITH 0x 
ORACLE_CONSUMER_ADDRESS=contract_address_here_with_0x_prefix

# Exchange Rate Configuration
COINGECKO_API_URL=https://api.coingecko.com/api/v3
RATE_CACHE_TTL_SECONDS=300
RATE_UPDATE_INTERVAL_MS=300000

# KYC Configuration
KYC_DEFAULT_MAX_TICKETS=4
KYC_EXPIRY_DAYS=365
KYC_HASH_SALT=blockticket_ganache_local_2025

# Security
JWT_SECRET=strong_jwt_secret_here
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173